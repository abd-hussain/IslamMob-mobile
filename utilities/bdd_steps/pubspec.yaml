name: bdd_steps
description: BDD Steps
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.4 <4.0.0"

# Add regular dependencies here.
dependencies:
  flutter_test:
    sdk: flutter
  golden_toolkit: ^0.15.0

dev_dependencies:
  islam_mob_lint:
    path: ../islam_mob_lint
  dependency_validator: 4.1.0
