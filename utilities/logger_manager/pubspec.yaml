name: logger_manager
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.4 <4.0.0"

dependencies:
  logger: ^2.4.0

dev_dependencies:
  lints: ^4.0.0
  json_serializable: 6.8.0
  dependency_validator: 4.1.0

flutter:
  generate: true
  uses-material-design: true
