name: location_manager
version: 1.0.0
publish_to: 'none' 

environment:
  sdk: ">=3.3.4 <4.0.0"
  
dependencies:
  flutter:
    sdk: flutter
  geocoding: ^3.0.0
  geolocator: ^13.0.3
  
  logger_manager:
    path: ../../utilities/logger_manager

dependency_overrides:
  geolocator_android: ^4.7.5
  
dev_dependencies:
  build_runner: 2.4.12
  dependency_validator: 4.1.0
  islam_mob_lint:
    path: ../islam_mob_lint
  json_serializable: 6.8.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
