name: internet_connection_checkup
version: 1.0.0
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
environment:
  sdk: '>=3.9.2 <4.0.0'

resolution: workspace

dependencies:
  connectivity_plus: ^6.1.3

  logger_manager: any

dev_dependencies:
  islam_mob_lint: any
  test: ^1.24.0
  mocktail: 1.0.4
  dependency_validator: 5.0.2

flutter:
  generate: true
  uses-material-design: true