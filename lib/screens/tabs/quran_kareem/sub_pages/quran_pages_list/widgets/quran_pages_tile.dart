import 'package:flutter/material.dart';

class QuranPagesTileView extends StatelessWidget {
  const QuranPagesTileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
