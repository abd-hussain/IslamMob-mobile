import 'package:flutter/material.dart';

class PartTileView extends StatelessWidget {
  const PartTileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
