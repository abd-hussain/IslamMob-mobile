
name: utilities

packages:
  - utilities/**
  - .

scripts:
  analyze:
    exec: dart analyze .
  pub-get:
    run: melos exec -- "flutter pub get"

command:
  bootstrap:
    usePubspecOverrides: true